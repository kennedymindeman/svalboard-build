# Log

## 2026-08-16

- Bundle created by splitting `discord/general-channel.md` (the distilled Discord #general reference) into one page per topic. From its section 1 plus the file's source paragraph and the closing "Gaps" section: [discord-general-overview](/discord-general-overview.md). From section 2's subsections: [firmware-and-config](/firmware-and-config.md), [printing](/printing.md), [pointing-devices](/pointing-devices.md), [ergonomics-and-fit](/ergonomics-and-fit.md), [gaming](/gaming.md), [suppliers-and-parts](/suppliers-and-parts.md). From sections 3–6: [timeline](/timeline.md), [open-questions](/open-questions.md), [links](/links.md), [faq](/faq.md). No sections were small enough to merge — Gaming, the smallest at ~10 lines, kept its own page.
- Content moved verbatim; the only edits were dropping the "Source:" prefix now that it sits under a heading, qualifying two channel-relative phrases, and adding per-page frontmatter (`source: discord #general …`) plus the message-id note at the top of each page. `discord/general-channel.md` is now a stub pointing here.

## 2026-08-16 (later)

- Backfilled every page from a second Discord capture covering 2023-09-07..2025-08-10 (32,164 messages; total now 41,085 of ~48,135, one gap left at 2025-08-10..2025-10-17). Pipeline: `tools/har2jsonl.py` (now ignores the `/messages/pins` endpoint) → `tools/chunk.py` (new; 25 chunks of ~1,287) → one distillation per chunk in the `distill-1.md` format → sliced by section into per-topic files → one merge per wiki page. Every page kept all of its pre-merge citation ids (checked per page: 0 missing); pages grew 3-9x. Superseded positions (34/40 mm balls, Vial-only, filament budget, resin/SLA, key-force tables) are kept as dated history rather than dropped; [timeline](/timeline.md) now starts 2023-09 and marks the gap; [discord-general-overview](/discord-general-overview.md) gained an era guide and a voices-over-time section.
- Frontmatter: `source:` on every page now reads `2023-09-07..2026-08-15 (gap 2025-08-10..2025-10-17)`; `title`/`description` values containing `#general` are quoted so YAML doesn't truncate them; the message-id note on each page now warns that 6-digit suffixes can collide in the 41k-message export.

## 2026-08-22

- Folded in the channel's founding months, 2023-06-30..2023-09-06 (1,776 messages, four chunks read in full; total now 42,861 of ~48,135, one gap left at 2025-08-10..2025-10-17): [timeline](/timeline.md) gained a "June – early September 2023" section, [open-questions](/open-questions.md) gained the hall-effect sensing thread (new to the wiki), and [printing](/printing.md), [firmware-and-config](/firmware-and-config.md), [suppliers-and-parts](/suppliers-and-parts.md), [ergonomics-and-fit](/ergonomics-and-fit.md), [pointing-devices](/pointing-devices.md), [faq](/faq.md) and [links](/links.md) gained founding-era facts, with superseded positions (Loctite 638, left-as-master, SLA keysets) kept as dated history; [gaming](/gaming.md) had nothing to take, so its `source:` still starts 2023-09-07.

## 2026-08-29

- Added a second OKF bundle, [thecore/index.md](thecore/index.md), distilled from TheCore StarCraft II hotkey Discord #general (23,419 messages, 2019-08-24..2026-08-27; 18 chunks → per-chunk distillations → topic slices → one merge per page, all on claude-opus-5). Ten pages; the hotkey-file page opens with the binds the in-game editor refuses but the text file accepts, and with how the game rewrites the file per game mode. Added [svalboard-and-thecore](/svalboard-and-thecore.md) reading the two communities together; the Svalboard channel has almost no StarCraft content, so its implications are marked as derived. Svalboard #general capture extended to 2026-08-22 (43,129 messages; the last 268 are not yet distilled); channel `1172312033199407114` captured (8,943 messages) but not distilled.
- Later the same day: captured the whole of TheCore #hardware (2,602 messages, 2019-01-05..2026-06-04) and folded it into [thecore/keyboards-and-hardware.md](thecore/keyboards-and-hardware.md) with `H:`-prefixed ids; [svalboard-and-thecore](/svalboard-and-thecore.md) gained the Azeron finger-cluster builds and the subgroup-key answer. Neither TheCore channel mentions DataHand, Svalboard or Lalboard.
- Later again: fetched the TheCore Google Drive folder and kept two `.SC2Hotkeys` files and two 5.0 diagrams under [`thecore/`](../thecore/README.md) (provenance in its README; handbook and spreadsheets stay out of the repo). Added `tools/thecore_keys.py` (per-key and per-finger binding load of a hotkey file) and [thecore-method-on-a-svalboard](/thecore-method-on-a-svalboard.md): TheCore's method as rules with quotes from its own documents, the Svalboard wiki's per-position ease evidence, and a derived recipe plus worked assignment for the Svalboard's geometry. Fixed a citation on [ergonomics-and-fit](/ergonomics-and-fit.md): the "no sixth up key" quotes (410610, 139005) are about finger clusters, not thumbs; added 891237. A blind check of the new page's citations also re-pointed inherited ids: [ergonomics-and-fit](/ergonomics-and-fit.md) (498001 dropped for the inward/outward claim; the three difficulty rankings 660969, 456597, 159072 disagree about north and now say so), [gaming](/gaming.md) (489416 → 244127 for the Wooting 6.9 cps) and [firmware-and-config](/firmware-and-config.md) (056916 added for the centre-click default; 057237 only says south). Added `tools/thecore_keymap.py` and the generated [`thecore/keymap.html`](../thecore/keymap.html): a self-contained page showing which abilities and units sit on each key of the two shipped `.SC2Hotkeys` files, toggled by melee race or co-op commander (units placed by an explicit 375-unit table; commander views include all melee units of the race, which over-includes). Added `tools/thecore_svalboard.py` and [`thecore/svalboard-keymap.html`](../thecore/svalboard-keymap.html): the same bindings drawn on two Svalboard hands under a fixed TheCore-key to Svalboard-key mapping.
- Later still: measured the input load the layout has to carry. `tools/sc2_sequences.py` reads pro replays with sc2reader and writes an event stream, then aggregates it into [sc2-command-sequences](/sc2-command-sequences.md) and `thecore/sequences-summary.json` (committed, so the page rebuilds without the replays). Set: IEM Katowice 2024 main event, 187 games on patch 5.0.12.91115, all parsed; provenance and refetch commands in `replays/README.md`, replay files stay out of the repo. Headline: control-group recalls dwarf everything else, and TheCore's group keys sit mostly on one finger, so same-finger repetition runs 27% (Terran) to 36% (Zerg) of consecutive within-1s pairs, of which 22% (Terran, Protoss) to 32% (Zerg) is the same finger on a *different* key — the part a layout can do something about.
- Later still: replaced the two-hand Svalboard mapping. The user plays left hand on the board and right hand on an ordinary mouse, so the second hand is gone and the room it gave comes from a held Nail layer instead (4c's own alternative). `tools/thecore_svalboard.py` no longer carries a hand-written mapping: it ranks TheCore keys by replay load, places them greedily into 40 slots (20 base, 20 layer) ordered by 4a's zones, then runs a hill climb that trades same-finger bigram cost against zone cost, and prints the whole run. Section 4d of [thecore-method-on-a-svalboard](/thecore-method-on-a-svalboard.md) is now that generated table, and the page shows the Vial keycode per slot because only the firmware changes, never the hotkey file. Section 2's binding counts corrected to 1,434 and 1,314.

## 2026-08-30

- Extended the same measurement to co-op. `tools/sc2_sequences.py extract --coop` groups by commander instead of melee race and keeps only the human players; `report --coop` writes [sc2-coop-sequences](/sc2-coop-sequences.md) and `thecore/coop-summary.json`. Set: the starcraft2coop.com speedrun archive, 977 replay files across 59 game builds — 33 byte-identical duplicates and 1 misfiled replay skipped, 943 parsed, none failed; 1,089 player-games, 18 commanders, 13,806 minutes. Three things the 1v1 set could not show. Commanders differ far more than races do: 27-46 commands a minute, but 13 (Han & Horner) to 76 (Raynor) control-group actions a minute, and same-finger-different-key runs 5.6% (Mengsk) to 61.4% (Raynor) of pairs — though each commander's rows come from only 3-19 distinct players, mostly a couple of runners each, so that spread may be the runner rather than the commander. A third (31.5%) of co-op commands have no name in sc2reader and are kept as `ability:0x....` ids, which count and sequence fine but cannot be mapped to a key, so per-commander key coverage runs 16.0% (Zeratul) to 47.5% (Artanis), with Mengsk at 21.6%; the bigger part of that gap is right-clicks, which are a mouse action. And a "Solo" run in the archive still has a second human slot with an idle ally (usually a Raynor who never moves), so in a `<Commander> Solo` directory only that commander's player is counted.

## 2026-08-31

- Adopted a normalized 50/50 1v1+co-op blend as the layout the page ships (#27, PR #30). `tools/thecore_svalboard.py` gained `--coop-blend W` and `--coop-normalize`: the co-op corpus is an equal-weight mean over all 18 commanders, never pooled, then scaled to the 1v1 corpus's total mass (kscale 5.324526 on load, pscale 9.649375 on the bigram dict) before mixing, so the blend weighs the two corpora by choice rather than by how many replays each happens to contain. `tools/coop_blend_report.py` and the committed [`thecore/coop-blend-report.md`](../thecore/coop-blend-report.md) quantify all three variants under both objectives. The 1v1 cost the blend gives up is small and the co-op gain is large, which is why it is the default the page and `thecore/svalboard-keymap.html` now show.
- Researched where the same-finger collisions land, since some are unavoidable and the useful question is which ones (#28, PR #32). Added `direction_class()` (same-direction, centre-edge, adjacent-edges, opposite-edges), the optional `--direction-tiers EASY,MED,HARD` multipliers that compose with the existing 0.5 cross-plane factor, `tools/collision_report.py` and the committed [`thecore/collision-report.md`](../thecore/collision-report.md). Result: the collisions already sit on the easy class — centre-edge carries 20.9-24.2 of roughly 30 collisions a minute — and re-running with tiers moves zero keys on the adopted layout at every tier setting. The flag stays off by default and no committed artifact changed.
- Fixed Idle Worker and Town Camera falling off the hand (#33, PR #34). The cause was placement order, not capacity: `assign()` ordered keys by replay load alone, so a zero-load unconstrained key could take the last middle- or ring-finger slot that a `group`-class key is the only kind allowed to use. `ROLE_TIGHTNESS` now sorts most-constrained-role-first (control-group recall, then the camera/idle-worker group, then command-card keys, then free) before load. Both keys are seated at essentially no cost: 5.0 Right Plus is unchanged at 83.67, 6.0 Right goes 82.58 → 82.60. It is a trade, not new room — 5.0 now drops `X` (Merc Hellion, Set Bunker Rally Point) and `B` (Stop Planetary Fortress); 6.0 drops `R` (Vespene Drone) and `Space` (Stalker Hallucination). The prior hypothesis that modifier keys should let far more than 40 bindings onto the hand is false, in three independent ways: SC2 accepts modifiers only on the global tab and refuses them on command-card binds; TheCore already spends the whole modifier space (53 keys carry 1,434 bindings in 5.0); and a scan of all eight modifier states found no free non-Alt combination on any middle or ring key in either file. Real headroom is 2 slots in 5.0 and 5 in 6.0.

## 2026-09-01

- **svalboard-build #35 / PR #36: three exclusion rules make the demand fit.** Both TheCore files asked for more keys
  than the left hand has slots (42 and 45 against 40). `exclude()` now drops rally-only keys (a right-click already
  sets a rally, so `F` and `Z` go in both files), chat keys (typing the message needs the right hand anyway, so
  `Enter`/`Tab` in 5.0 and `Enter`/`` ` `` in 6.0 go), and the redundant second holder of `MenuGame` (`F10` in 5.0,
  `F3` in 6.0, both keeping `Escape`). Demand falls to 37 and 40, nothing is unplaced, and `X`, `B`, `R`, `Space` and
  6.0's `Escape` come back onto the hand alongside Idle Worker and Town Camera. The mouse and right-hand rules are
  design decisions about this build, not measurements; the wiki says so. A key only goes when *every* non-banished
  binding is covered, so `D` keeps its slot despite carrying `RallyEgg`. Dropping a key takes its banished bindings
  too: `StatusEnemy`, `WarpIn` and `QuickSave` lose their Pad+Down+Knuckle route.
- **No `.SC2Hotkeys` edit was needed.** The alternative was moving single-binding global commands onto free modifier
  combos of already-placed keys, which measurement said was possible (24 must-stay keys in 5.0 have 61 free
  single-modifier pairs against 67 binds to relocate). The three exclusions were enough on their own, so section 4e's
  rule that only the firmware changes still holds.
- **Banished keys were never the headroom.** `exclude()` already dropped keys whose every binding is Ctrl+Shift+Alt
  (5 in 5.0, 7 in 6.0), so they never entered the 40-slot demand. Routing banished bindings to F13-F24 would empty
  zero additional keys. The F13-F24 escape hatch stays documented and untaken.
